const doctorController = {
    createDoctor: async (req, res) => {
        try {
            const {
                exp,
                contactInfo,
                landline,
                hospital,
                section,
            } = req.body

            const 
        }
        catch (err) {
            console.log(err)
        }
    }
};

module.exports = doctorController;